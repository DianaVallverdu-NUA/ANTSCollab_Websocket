[project]
name = "fax-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "websockets>=12.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["serve"]